/**
 * Template Type Definitions
 */

export interface LaunchNotesTemplate {
  id: string;
  name: string;
  headline?: string;
  description?: string;
  excerpt?: string;
  createdAt: string;
  updatedAt: string;
}
